import { Navbar } from "nextra-theme-docs";

const NavbarComponent = () => {
  return (
    <div className="page-px">
      <Navbar logo={"SlopScore.ai"} />
    </div>
  );
};

export default NavbarComponent;
